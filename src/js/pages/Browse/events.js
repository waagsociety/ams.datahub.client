// import { query, search } from '../../actions'

const Browse = ({ dispatch }) => ({

  onSubmit: (event) => {
    event.preventDefault() 
  },

  onChange: (event) => {
    // 
  },

})

export default Browse